# 初始化 SessionBox AI 上下文文档（index + claude/ 拆分）

## 目标
按命令规范，把现有"超大单体 CLAUDE.md"（根 16KB、electron/src/preload 各一份）拆分为：每个目录一个轻量 `CLAUDE.md` 索引 + 一个 `claude/` 子目录承载 11 个详情文件。**关键：文档必须反映当前代码现状**（用户已确认），不沿用 2026-04-17 旧文档中的过时内容。

## 目标目录与文件清单（4 个目录 × 12 文件 = 48 个文件）

对 **根目录**、`electron/`、`preload/`、`src/` 各生成：

```
<target>/CLAUDE.md            # 索引：简介 + 高优先级约定 + 文件索引表 + (根含模块索引+Mermaid) + 扫描状态
<target>/claude/overview.md
<target>/claude/conventions.md
<target>/claude/module-responsibilities.md
<target>/claude/entrypoints.md
<target>/claude/public-interfaces.md
<target>/claude/dependencies-and-config.md
<target>/claude/data-model.md
<target>/claude/testing-and-quality.md
<target>/claude/file-map.md
<target>/claude/faq.md
<target>/claude/changelog.md
```

无事实依据的章节会写明"未发现"，不编造。preload 体量小，详情文件保持精简但齐全。

## 已核实的当前代码事实（文档依据，已纠正旧文档错误）
- **Workflow 功能已从 renderer+preload 移除**：`src/lib/workflow/`、workflow store、workflow 组件目录、preload 的 4 个 workflow 命名空间均不存在 → 文档不写 workflow（用户选"仅反映当前代码"）。
- **新增调试器/自动化系统**：`electron/services/debugger.ts`（rrweb 录制/回放，CDN 注入）、`action-recorder.ts` + `action-player.ts`（确定性 DOM 操作录制/回放）、`electron/ipc/debugger.ts`（18 个 `debugger:*` 通道）、3 个调试器预加载入口 + `debugger-window.html`/`debugger-replay.html`。
- **webview-manager 重构**：`electron/services/webview-manager.ts` 单例保留，逻辑委托给 `webview/` 7 个子模块（types/blocked-protocols/proxy/events/freeze/sniffer/zoom）。
- **preload 实际 32 个 api 命名空间**（非旧的 33）：含未文档化的 `theme`/`system`/`debugger`，无 workflow 系列。
- **store.ts** schema keys 与模型已核实（含 `zoomPreferences`、`activeUpdateSourceId` 等旧文档遗漏项）；Bookmark/Password 在独立 JsonStore。
- **components/** 含 `containers`、`proxy` 子目录，无 `workflow`；stores 共 21 个（无 workflow）。
- **ESLint**：`eslint.config.js` 有 `max-lines 1000` / `max-lines-per-function 200`（warn）、放宽 TS 规则；无测试框架。
- **构建**：`electron.vite.config.ts` 三个入口 + 自定义插件复制 chrome-extension preload 与 debugger 窗口 HTML；TS 三套 tsconfig。

## 各详情文件主要内容
- **根**：overview=三进程架构+数据流；conventions=命令/IPC 三处同步/数据模型同步/无边框窗口；module-responsibilities=4 模块职责；entrypoints=构建配置+启动总流程；public-interfaces=自定义协议(sessionbox/account-icon/extension-icon/site-icon/screenshot)+默认浏览器+MCP 外部端点(9527)；dependencies-and-config=核心依赖+tsconfig+electron-builder；data-model=核心领域模型关系+四层存储(electron-store/JsonStore/Dexie/文件目录)；testing-and-quality=无测试+ESLint 规则；file-map=根目录文件清单；faq=21 条"改 X 看 Y"导航+关键注意点。
- **electron**：public-interfaces=全部 IPC 通道(含新 debugger:*)+主进程→渲染进程事件+协议处理；data-model=store.ts schema 全表+JsonStore 文件+MCP 工具+Skill Store；entrypoints=main.ts 9 步启动序列；module-responsibilities=services/ipc/utils/composables 分域；faq=服务层导航。
- **preload**：public-interfaces=32 个命名空间全表+导出类型；conventions=contextBridge+三处同步；其余精简。
- **src**：public-interfaces=内部页面(sessionbox://*)+21 Stores 表+composables 表；data-model=types+Dexie(sessionbox-history/chat)+chat-store 作用域工厂(依据 docs 设计文档标注)；entrypoints=main.ts/App.vue；module-responsibilities=components/stores/composables/lib/types；faq=UI 导航。

## 执行步骤
1. 用 TodoWrite 建任务跟踪（4 目录 × 写详情→写索引）。
2. 先写各目录 `claude/*.md` 详情文件，再写 `CLAUDE.md` 索引（保证链接落到真实文件）。
3. 链接用相对路径；文件名 kebab-case。
4. `changelog.md` 仅保留最近 5 条、时间倒序；根 changelog 记录本次"按 index+detail 策略重构并同步代码现状"。
5. 不改任何源代码；不执行 git 操作。

## 覆盖率与缺口
- 已扫描：根、electron(主进程)、preload、src(渲染进程) 全部源码结构与关键文件。
- 未深挖（建议下一步）：`src/components/**` 内单个 .vue 组件（250+，本次仅按目录归纳）；`docs/superpowers/**` 设计文档（本次仅在 data-model/faq 引用相关项）；`scripts/`、`plugins/` 构建脚本细节。
- 主要缺口：无测试，故 testing 文件如实标注。
