# Tray 菜单功能实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 SessionBox 添加系统托盘菜单，支持从 Tray 快速打开页面（软件内/新窗口/任务栏桌面版/任务栏手机版）。

**Architecture:** 新增 `tray.ts` 管理 Tray 生命周期和菜单构建，新增 `tray-window.ts` 负责独立 BrowserWindow 的创建与定位。修改 `main.ts` 将窗口关闭行为从直接退出改为隐藏到托盘。通过 IPC `tray:openInApp` 通知渲染进程打开页面。

**Tech Stack:** Electron Tray/Menu API, BrowserWindow, WebContentsView, IPC

---

## 文件结构

| 操作 | 文件 | 职责 |
|------|------|------|
| 创建 | `electron/services/tray-window.ts` | 独立窗口工厂：创建新窗口和任务栏窗口，管理窗口生命周期 |
| 创建 | `electron/services/tray.ts` | Tray 管理器：创建 Tray、构建菜单、监听数据变化、分发点击事件 |
| 修改 | `electron/main.ts` | 集成 Tray，改变窗口关闭行为 |
| 修改 | `preload/index.ts` | 添加 `tray:openInApp` 事件监听支持 |
| 修改 | `src/stores/tab.ts` | 处理 `tray:openInApp` 事件，打开指定页面 |
| 创建 | `resources/trayIconTemplate@2x.png` | macOS 模板图标 |
| 创建 | `resources/trayIcon.png` | Windows 彩色图标 |

---

### Task 1: 准备 Tray 图标资源

**Files:**
- 创建: `resources/trayIconTemplate@2x.png`（macOS 模板图标，32×32 单色）
- 创建: `resources/trayIcon.png`（Windows 图标，16×16 彩色）

- [ ] **Step 1: 从现有 icon.png 生成 Tray 图标**

使用现有 `resources/icon.png` 缩放生成两个图标文件。在项目根目录运行：

```bash
# macOS: 使用 sips 生成 16x16 和 32x32 图标
sips -z 16 16 resources/icon.png --out resources/trayIcon.png
sips -z 32 32 resources/icon.png --out resources/trayIconTemplate@2x.png
```

- [ ] **Step 2: 将 macOS 图标转为模板图标（灰度）**

macOS Tray 图标需要是模板图标（黑色图案 + 透明背景），以便自动适配亮色/暗色菜单栏。使用 ImageMagick 或手动编辑将 `trayIconTemplate@2x.png` 转为灰度模板图标。如果 ImageMagick 可用：

```bash
convert resources/trayIconTemplate@2x.png -colorspace gray -threshold 50% -negate resources/trayIconTemplate@2x.png
```

如果不可用，手动用图片编辑器将图标转为纯黑+透明即可。

- [ ] **Step 3: 提交图标文件**

```bash
git add resources/trayIcon.png resources/trayIconTemplate@2x.png
git commit -m "chore: add tray icon resources"
```

---

### Task 2: 创建独立窗口工厂 (tray-window.ts)

**Files:**
- 创建: `electron/services/tray-window.ts`

- [ ] **Step 1: 创建 tray-window.ts 基础结构**

```typescript
// electron/services/tray-window.ts
import { BrowserWindow, WebContentsView, Tray, screen } from 'electron'
import { join } from 'path'
import type { Page } from './store'

class TrayWindowManager {
  private windows = new Set<BrowserWindow>()

  /** 创建新窗口打开指定页面 */
  openInNewWindow(page: Page): BrowserWindow {
    const containerId = page.containerId || ''
    const partition = containerId ? `persist:container-${containerId}` : undefined

    const win = new BrowserWindow({
      width: 1280,
      height: 800,
      show: false,
      autoHideMenuBar: true,
      title: page.name || '新窗口',
      webPreferences: {
        partition,
        sandbox: false
      }
    })

    win.loadURL(page.url || 'about:blank')
    win.once('ready-to-show', () => win.show())

    this.trackWindow(win)
    return win
  }

  /** 创建贴近任务栏的窗口 */
  openAtTaskbar(tray: Tray, page: Page, mode: 'desktop' | 'mobile'): BrowserWindow {
    const containerId = page.containerId || ''
    const partition = containerId ? `persist:container-${containerId}` : undefined

    const sizes = {
      desktop: { width: 480, height: 270 },
      mobile: { width: 270, height: 480 }
    }
    const { width, height } = sizes[mode]

    const win = new BrowserWindow({
      width,
      height,
      show: false,
      frame: false,
      resizable: true,
      alwaysOnTop: true,
      skipTaskbar: true,
      webPreferences: {
        partition,
        sandbox: false
      }
    })

    // 定位到 Tray 图标附近
    this.positionNearTray(win, tray, width, height)

    win.loadURL(page.url || 'about:blank')
    win.once('ready-to-show', () => win.show())

    // 失焦自动隐藏
    win.on('blur', () => {
      if (!win.isDestroyed()) win.hide()
    })

    this.trackWindow(win)
    return win
  }

  /** 将窗口定位到 Tray 图标附近 */
  private positionNearTray(win: BrowserWindow, tray: Tray, width: number, height: number): void {
    try {
      const trayBounds = tray.getBounds()
      const display = screen.getDisplayNearestPoint({
        x: trayBounds.x + trayBounds.width / 2,
        y: trayBounds.y + trayBounds.height / 2
      })
      const { workArea } = display

      // 默认在 Tray 图标上方显示
      let x = Math.round(trayBounds.x + trayBounds.width / 2 - width / 2)
      let y = Math.round(trayBounds.y - height)

      // 如果超出屏幕上方，改为下方
      if (y < workArea.y) {
        y = trayBounds.y + trayBounds.height
      }

      // 确保不超出屏幕左右边界
      x = Math.max(workArea.x, Math.min(x, workArea.x + workArea.width - width))
      y = Math.max(workArea.y, Math.min(y, workArea.y + workArea.height - height))

      win.setPosition(x, y)
    } catch {
      // 获取位置失败时居中显示
      win.center()
    }
  }

  /** 跟踪窗口生命周期 */
  private trackWindow(win: BrowserWindow): void {
    this.windows.add(win)
    win.on('closed', () => {
      this.windows.delete(win)
    })
  }

  /** 销毁所有独立窗口 */
  destroyAll(): void {
    for (const win of this.windows) {
      if (!win.isDestroyed()) {
        win.destroy()
      }
    }
    this.windows.clear()
  }
}

export const trayWindowManager = new TrayWindowManager()
```

- [ ] **Step 2: 提交**

```bash
git add electron/services/tray-window.ts
git commit -m "feat(tray): add tray window manager for independent browser windows"
```

---

### Task 3: 创建 Tray 管理器 (tray.ts)

**Files:**
- 创建: `electron/services/tray.ts`

- [ ] **Step 1: 创建 tray.ts**

```typescript
// electron/services/tray.ts
import { app, Tray, Menu, nativeImage, BrowserWindow } from 'electron'
import { join } from 'path'
import { listGroups, listPages } from './store'
import { trayWindowManager } from './tray-window'
import type { Page } from './store'

class TrayManager {
  private tray: Tray | null = null

  init(mainWindow: BrowserWindow): void {
    const iconPath = this.getIconPath()
    const icon = nativeImage.createFromPath(iconPath)

    // macOS 使用模板图标自动适配主题
    if (process.platform === 'darwin') {
      icon.setTemplateImage(true)
    }

    this.tray = new Tray(icon)
    this.tray.setToolTip('SessionBox')

    // 左键点击显示/隐藏主窗口
    this.tray.on('click', () => {
      if (mainWindow.isVisible()) {
        mainWindow.hide()
      } else {
        mainWindow.show()
        mainWindow.focus()
      }
    })

    // 右键显示菜单
    this.tray.on('right-click', () => {
      const menu = this.buildMenu(mainWindow)
      this.tray!.popUpContextMenu(menu)
    })

    // 初始化菜单
    this.tray.setContextMenu(this.buildMenu(mainWindow))

    // 监听 store 数据变化，刷新菜单
    this.watchStoreChanges(mainWindow)
  }

  private getIconPath(): string {
    if (app.isPackaged) {
      const name = process.platform === 'darwin' ? 'trayIconTemplate@2x.png' : 'trayIcon.png'
      return join(process.resourcesPath, name)
    }
    const name = process.platform === 'darwin' ? 'trayIconTemplate@2x.png' : 'trayIcon.png'
    return join(__dirname, '../../resources', name)
  }

  private buildMenu(mainWindow: BrowserWindow): Menu {
    const groups = listGroups()
    const pages = listPages()

    // 构建分组 → 页面子菜单
    const pageSubmenu: Electron.MenuItemConstructorOptions[] = []

    if (pages.length === 0) {
      pageSubmenu.push({
        label: '暂无页面',
        enabled: false
      })
    } else {
      for (const group of groups) {
        const groupPages = pages
          .filter(p => p.groupId === group.id)
          .sort((a, b) => a.order - b.order)

        if (groupPages.length === 0) continue

        pageSubmenu.push({
          label: group.name,
          submenu: groupPages.map(page => ({
            label: page.name,
            submenu: [
              {
                label: '软件内打开',
                click: () => this.openInApp(mainWindow, page)
              },
              {
                label: '新窗口打开',
                click: () => trayWindowManager.openInNewWindow(page)
              },
              {
                label: '任务栏打开（桌面版）',
                click: () => {
                  if (this.tray) trayWindowManager.openAtTaskbar(this.tray, page, 'desktop')
                }
              },
              {
                label: '任务栏打开（手机版）',
                click: () => {
                  if (this.tray) trayWindowManager.openAtTaskbar(this.tray, page, 'mobile')
                }
              }
            ]
          }))
        })
      }

      // 没有分组的页面
      const ungroupedPages = pages
        .filter(p => !groups.some(g => g.id === p.groupId))
        .sort((a, b) => a.order - b.order)

      if (ungroupedPages.length > 0) {
        pageSubmenu.push({ type: 'separator' })
        for (const page of ungroupedPages) {
          pageSubmenu.push({
            label: page.name,
            submenu: [
              {
                label: '软件内打开',
                click: () => this.openInApp(mainWindow, page)
              },
              {
                label: '新窗口打开',
                click: () => trayWindowManager.openInNewWindow(page)
              },
              {
                label: '任务栏打开（桌面版）',
                click: () => {
                  if (this.tray) trayWindowManager.openAtTaskbar(this.tray, page, 'desktop')
                }
              },
              {
                label: '任务栏打开（手机版）',
                click: () => {
                  if (this.tray) trayWindowManager.openAtTaskbar(this.tray, page, 'mobile')
                }
              }
            ]
          })
        }
      }
    }

    return Menu.buildFromTemplate([
      {
        label: '打开页面',
        submenu: pageSubmenu
      },
      { type: 'separator' },
      {
        label: '打开主窗口',
        click: () => {
          mainWindow.show()
          mainWindow.focus()
        }
      },
      { type: 'separator' },
      {
        label: '退出',
        click: () => {
          trayWindowManager.destroyAll()
          app.quit()
        }
      }
    ])
  }

  /** 软件内打开：激活主窗口并通知渲染进程 */
  private openInApp(mainWindow: BrowserWindow, page: Page): void {
    mainWindow.show()
    mainWindow.focus()
    mainWindow.webContents.send('tray:openInApp', page.id)
  }

  /** 监听 store 变化刷新菜单 */
  private watchStoreChanges(mainWindow: BrowserWindow): void {
    // 使用 electron-store 的 onDidAnyChange 监听变化
    // 由于 store 是模块内部的，这里用定期刷新或 IPC 触发
    // 实际上 electron-store 实例不在本模块，改为在 main.ts 中手动调用 refreshMenu
  }

  /** 手动刷新菜单（由 main.ts 在数据变更时调用） */
  refreshMenu(mainWindow: BrowserWindow): void {
    if (!this.tray) return
    this.tray.setContextMenu(this.buildMenu(mainWindow))
  }

  destroy(): void {
    if (this.tray) {
      this.tray.destroy()
      this.tray = null
    }
  }
}

export const trayManager = new TrayManager()
```

- [ ] **Step 2: 提交**

```bash
git add electron/services/tray.ts
git commit -m "feat(tray): add tray manager with dynamic menu building"
```

---

### Task 4: 修改 main.ts 集成 Tray

**Files:**
- 修改: `electron/main.ts`

- [ ] **Step 1: 导入 trayManager 和 trayWindowManager**

在 `electron/main.ts` 顶部导入区域添加：

```typescript
import { trayManager } from './services/tray'
import { trayWindowManager } from './services/tray-window'
```

插入位置：在第 8 行 `import { registerGlobalShortcuts, ...` 之后。

- [ ] **Step 2: 修改窗口关闭行为（隐藏到托盘而非退出）**

将 `mainWindow.on('close', ...)` 回调改为拦截关闭事件，隐藏窗口到托盘。需要引入一个标志变量区分"真正退出"和"隐藏到托盘"。

在 `createWindow()` 函数内，`mainWindow` 创建之前添加标志变量：

```typescript
let isQuitting = false

app.on('before-quit', () => {
  isQuitting = true
  trayWindowManager.destroyAll()
})
```

然后将现有的 `mainWindow.on('close', ...)` 回调（第 184-196 行）替换为：

```typescript
mainWindow.on('close', (e) => {
  if (!isQuitting) {
    e.preventDefault()
    if (!mainWindow.isMaximized()) {
      const bounds = mainWindow.getBounds()
      const state = getWindowState()
      setWindowState({
        ...state,
        x: bounds.x,
        y: bounds.y,
        width: bounds.width,
        height: bounds.height
      })
    }
    mainWindow.hide()
    return
  }
  // 真正退出时保存状态
  if (!mainWindow.isMaximized()) {
    const bounds = mainWindow.getBounds()
    const state = getWindowState()
    setWindowState({
      ...state,
      x: bounds.x,
      y: bounds.y,
      width: bounds.width,
      height: bounds.height
    })
  }
})
```

- [ ] **Step 3: 在 `app.whenReady()` 中初始化 Tray**

在 `createWindow()` 调用之后（第 267 行之后），添加 Tray 初始化：

```typescript
// 初始化系统托盘
trayManager.init(mainWindow)
```

注意：`mainWindow` 需要能在 `whenReady` 回调中访问。由于 `createWindow()` 是在 `whenReady` 中调用的，且 `mainWindow` 是局部变量，需要将 `mainWindow` 提取为外层变量，或者在 `createWindow` 内部初始化 Tray。

推荐方案：在 `createWindow()` 函数内部末尾（`loadFile/loadURL` 之前），调用 Tray 初始化：

```typescript
// 初始化系统托盘
trayManager.init(mainWindow)
```

- [ ] **Step 4: 修改 `window-all-closed` 事件处理**

将第 284-289 行的 `window-all-closed` 事件处理改为不退出（因为主窗口关闭只是隐藏）：

```typescript
app.on('window-all-closed', () => {
  unregisterGlobalShortcuts()
  // 不再自动退出，用户通过 Tray 菜单退出
  // macOS 上保持默认行为
  if (process.platform !== 'darwin') {
    // Windows: 如果所有窗口都关闭了（包括独立窗口），可以选择退出或保留托盘
    // 这里保留托盘，用户通过 Tray 菜单退出
  }
})
```

- [ ] **Step 5: 在 Tray 菜单退出时调用 `mainWindow.close()` 触发真实关闭**

确保 `tray.ts` 中的"退出"菜单项调用 `app.quit()`（已实现），`before-quit` 事件中设置 `isQuitting = true`，这样 `mainWindow.close()` 不会再被拦截。

- [ ] **Step 6: 提交**

```bash
git add electron/main.ts
git commit -m "feat(tray): integrate tray into main process, minimize to tray on close"
```

---

### Task 5: 添加 preload IPC 事件支持

**Files:**
- 修改: `preload/index.ts`

- [ ] **Step 1: 在 `api.on` 方法中确认已支持自定义事件**

查看 `preload/index.ts` 第 416-421 行，`api.on` 方法是通用的：

```typescript
on: (event: string, callback: (...args: unknown[]) => void): (() => void) => {
  const channel = `on:${event}`
  const handler = (_e: Electron.IpcRendererEvent, ...args: unknown[]): void => callback(...args)
  ipcRenderer.on(channel, handler)
  return () => ipcRenderer.removeListener(channel, handler)
}
```

渲染进程可以这样监听：

```typescript
api.on('tray:openInApp', (pageId: unknown) => { ... })
```

主进程发送时使用：

```typescript
mainWindow.webContents.send('on:tray:openInApp', pageId)
```

**无需修改 preload。** 但需要确保 `tray.ts` 中发送的事件通道名与渲染进程监听的通道名一致。`tray.ts` 中应使用 `on:tray:openInApp`，而 `api.on('tray:openInApp', ...)` 会自动加 `on:` 前缀。

检查 Task 3 中 `tray.ts` 的 `openInApp` 方法，确认发送的通道名：

```typescript
mainWindow.webContents.send('on:tray:openInApp', page.id)
```

这与 `api.on('tray:openInApp', ...)` 匹配。无需额外修改。

- [ ] **Step 2: 验证无误后跳过提交**

---

### Task 6: 渲染进程处理 tray:openInApp 事件

**Files:**
- 修改: `src/stores/tab.ts`

- [ ] **Step 1: 在 tab store 的 `setupListeners` 中添加 tray 事件监听**

在 `src/stores/tab.ts` 的 `setupListeners()` 函数中（约第 557 行 `api.on('open-page', ...)` 之后），添加：

```typescript
// Tray 菜单打开页面
api.on('tray:openInApp', async (pageId: unknown) => {
  const id = pageId as string
  // 查找该页面是否已有打开的标签页
  const existingTab = sortedTabs.value.find(t => t.pageId === id)
  if (existingTab) {
    // 已有标签页，直接激活
    await switchTab(existingTab.id)
  } else {
    // 创建新标签页
    await createTab(id)
  }
})
```

- [ ] **Step 2: 提交**

```bash
git add src/stores/tab.ts
git commit -m "feat(tray): handle tray:openInApp event in tab store"
```

---

### Task 7: 配置 electron-builder 包含 Tray 图标

**Files:**
- 修改: `electron-builder.json`

- [ ] **Step 1: 确保 Tray 图标被打包**

检查 `electron-builder.json` 中的 `files` 或 `extraResources` 配置，确保 `resources/` 下的 tray 图标被包含在打包产物中。

需要在 `electron-builder.json` 中添加 `extraResources` 配置：

```json
{
  "extraResources": [
    {
      "from": "resources/trayIcon.png",
      "to": "trayIcon.png"
    },
    {
      "from": "resources/trayIconTemplate@2x.png",
      "to": "trayIconTemplate@2x.png"
    }
  ]
}
```

这样打包后图标路径为 `process.resourcesPath/trayIcon.png`，与 `tray.ts` 中 `getIconPath()` 的打包路径一致。

- [ ] **Step 2: 提交**

```bash
git add electron-builder.json
git commit -m "build: include tray icons in electron-builder package"
```

---

### Task 8: 验证与测试

- [ ] **Step 1: 运行开发模式验证**

```bash
pnpm dev
```

验证清单：
1. 应用启动后系统托盘出现图标
2. 左键点击 Tray 图标 → 主窗口显示/隐藏切换
3. 右键点击 Tray 图标 → 弹出菜单
4. 菜单中展示所有分组和页面的层级结构
5. 点击"软件内打开" → 主窗口激活并打开对应页面
6. 点击"新窗口打开" → 新 BrowserWindow 出现，加载对应 partition
7. 点击"任务栏打开（桌面版）" → 480×270 窗口紧贴 Tray 图标
8. 点击"任务栏打开（手机版）" → 270×480 窗口紧贴 Tray 图标
9. 任务栏窗口失焦后自动隐藏
10. 关闭主窗口 → 隐藏到托盘而非退出
11. 点击 Tray 菜单"退出" → 应用完全退出
12. 没有分组/页面时菜单显示"暂无页面"

- [ ] **Step 2: 构建验证**

```bash
pnpm build
```

确认构建无报错，Tray 图标路径在打包模式下正确。

- [ ] **Step 3: 最终提交**

```bash
git add -A
git commit -m "feat(tray): complete tray menu feature with all opening modes"
```
