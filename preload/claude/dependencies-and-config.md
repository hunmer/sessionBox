[根目录](../../CLAUDE.md) > [preload](../CLAUDE.md) > 详情

# preload/ 依赖与配置

| 依赖 | 用途 |
|------|------|
| `@electron-toolkit/preload` | 提供 `electronAPI`（暴露给 `window.electron`） |

构建配置见根 [dependencies-and-config.md](../../claude/dependencies-and-config.md)（`electron.vite.config.ts` 的 preload 段，含 chrome-extension preload 复制插件）。
